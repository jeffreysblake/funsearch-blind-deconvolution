[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --cov=backend
    --cov=config
    --cov=cli
    --cov-report=term-missing
    --cov-report=html
    --cov-report=xml
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow-running tests
    requires_docker: Tests that require Docker
    requires_lm_studio: Tests that require LM Studio
filterwarnings =
    ignore::DeprecationWarning
